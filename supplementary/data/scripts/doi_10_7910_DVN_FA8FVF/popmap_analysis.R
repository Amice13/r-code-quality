load("../data/popmap_ranks.RData")

########################################################################################################################################################################################################################
# Generate Correlations
########################################################################################################################################################################################################################
# Correlations  
cors <- cor(as.matrix(ranks[,-1]))

# Summary Stats on Correlations  
mean(cors[row(cors)!=col(cors)])
median(cors[row(cors)!=col(cors)])
range(cors[row(cors)!=col(cors)])[2] - range(cors[row(cors)!=col(cors)])[1]
